// Captone project
// Luka Sullivan
// December 10, 2024
//

let gameStarted = false;

function setup() {
createCanvas(window.Width, window.Height);

if (gameStarted === false) {
    showMenu();
}
}

function draw() {




function showMenu() {
    noStroke();
    textAlign(CENTER, CENTER);
    text("Puck Soccer", 10, 10);
    textSize(20);
}
}
